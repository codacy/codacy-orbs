description: "A default make job"

executor: machine

parameters:
  cmd:
    type: string
    description: "The Make command to run"

steps:
  - attach_workspace:
      at: ~/
  - run:
      name: Make command - << parameters.cmd >>
      command: make << parameters.cmd >>
