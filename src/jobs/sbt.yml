description: "A default sbt job"

executor: machine

parameters:
  cmd:
    description: "SBT command to run"
    type: string
  cache_prefix:
    description: "The prefix of cache to be used"
    type: string
    default: sbt-cache-032020
  aws_profile:
    description: "The AWS profile to be used"
    type: string
    default: "default"
  region:
    description: "The AWS region for the deployment"
    type: string
    default: eu-west-1
  credentials_file_content:
    description: "AWS credentials file contents to grant access for the step"
    type: string
    default: |
      [default]
      aws_access_key_id=$ACCESS_KEY_ID
      aws_secret_access_key=$SECRET_ACCESS_KEY
      [maven]
      source_profile = default
      role_arn = arn:aws:iam::$AWS_ACCOUNT_ID_PRODUCTION:role/$MAVEN_PUBLISHER_ROLE
  persist_to_workspace:
    description: "Whether to persist the workspace or not at the end of the job"
    type: boolean
    default: false
  store_test_results:
    description: "Whether to upload the test results back to CircleCI"
    type: boolean
    default: false
  install_sbt_version:
    description: "Version of SBT to install. If empty, the system one will be used"
    type: string
    default: ""
  install_ammonite_version:
    description: "Version of Ammonite to install (e.g. '2.4.0/2.13-2.4.0'). If empty then no version will be installed"
    type: string
    default: ""
  no_output_timeout:
    description: "The time to wait for the command without output"
    type: string
    default: "10m"

environment:
  AWS_PROFILE: << parameters.aws_profile >>
  AWS_DEFAULT_REGION: << parameters.region >>

steps:
  - attach_workspace:
      at: ~/workdir
  - when:
      condition: << parameters.install_sbt_version >>
      steps:
        - run:
            name: Download and install sbt << parameters.install_sbt_version >>
            command: |
              wget https://repo.scala-sbt.org/scalasbt/debian/sbt-<< parameters.install_sbt_version >>.deb
              sudo dpkg -i sbt-<< parameters.install_sbt_version >>.deb
              rm -f sbt-<< parameters.install_sbt_version >>.deb
  - when:
      condition: << parameters.install_ammonite_version >>
      steps:
        - run:
            name: Download and install Ammonite << parameters.install_ammonite_version >>
            command: |
              echo '#!/usr/bin/env sh' > amm
              curl -L https://github.com/com-lihaoyi/Ammonite/releases/download/<< parameters.install_ammonite_version >> >> amm
              chmod +x amm
              sudo mv amm /usr/local/bin/amm
  - run:
      name: Setup AWS Credentials
      command: |
        mkdir -p ~/.aws && touch ~/.aws/credentials
        cat >> ~/.aws/credentials \<< EOF
        << parameters.credentials_file_content >>
        EOF
  - run:
      name: Set OpenJdk
      command: sudo update-alternatives --set java /usr/lib/jvm/java-8-openjdk-amd64/jre/bin/java

  - run:
      name: Uncompress local targets
      command: |
        if [[ -f "targets.tar.gz" ]]; then
          echo "unpacking persisted workspace" && tar -xf targets.tar.gz
        else
          echo "no persisted workspace found"
        fi

  - when:
      condition: << parameters.store_test_results >>
      steps:
        - sbt_cached:
            cmd: << parameters.cmd >>
            cache_prefix: << parameters.cache_prefix >>
            store_test_results_path: ~/workdir
            no_output_timeout:  << parameters.no_output_timeout >>

  - unless:
      condition: << parameters.store_test_results >>
      steps:
        - sbt_cached:
            cmd: << parameters.cmd >>
            cache_prefix: << parameters.cache_prefix >>
            no_output_timeout:  << parameters.no_output_timeout >>

  - when:
      condition: << parameters.persist_to_workspace >>
      steps:
        - run:
            name: Compressing targets
            command: |
              find -name target -type d -exec tar -zcf targets.tar.gz -H posix {} + | true
              find -name target -type d -exec rm -rf {} + | true
        - persist_to_workspace:
            root: ~/workdir
            paths:
              - '*'
              - targets.tar.gz
