description: >
  A docker image to get the version out of Git history
  https://github.com/codacy/git-version

parameters:
  working_directory:
    type: string
    default: "~/workdir"
    description: Overrides the default working_directory.

docker:
  - image: codacy/git-version:latest

working_directory: <<parameters.working_directory>>
