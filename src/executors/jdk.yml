description: >
  Docker image to use JDK

docker:
  - image: 'cimg/openjdk:<< parameters.tag >>'

parameters:
  tag:
    description: >
      OpenJDK image version tag
    type: string
    default: '8.0.322'

working_directory: ~/workdir
